import { Menu } from "lucide-react";

const Navbar = () => {
    return (
        <div className="content-center p-3">
            <Menu color="white" />
        </div>
    );
};

export default Navbar;
